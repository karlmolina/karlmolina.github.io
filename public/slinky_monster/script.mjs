/**
 * Settings should include:
 * - no fill
 * - color fill
 * - speed of rotation
 * - radius of rotation
 * - size of circle
 * - square or circle
 * - inverted colors (black background)
 * - amount of circles to draw per frame
 * - stroke weight
 */

import sketchUtils from '../../src/utils/sketchUtils.mjs'

new p5(sketchUtils.wrapSketch())
